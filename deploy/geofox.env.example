# Install as /srv/haltewecker/secrets/geofox-hamburg/.env or inject these variables
# into the static-departures-api service. Never commit real values.
GEOFOX_USER=
GEOFOX_PASS=
# Optional for a controlled test endpoint; production defaults to Geofox GTI.
# GEOFOX_API_BASE_URL=https://gti.geofox.de/gti/public
